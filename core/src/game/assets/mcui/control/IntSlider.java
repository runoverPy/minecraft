package game.assets.mcui.control;

import game.util.relay.ObjectRelay;

public class IntSlider extends Slider<Integer> {
    public IntSlider(ObjectRelay<Integer> outerValue, Slider.Transformer<Integer> transformer) {
        super();
    }
}
